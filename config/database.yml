# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  database: igf_development
  username: root
  password:
  host: localhost
  socket: /tmp/mysql.sock
  
test:
  adapter: mysql
  database: igf_test
  username: root
  password:
  host: localhost
  socket: /tmp/mysql.sock